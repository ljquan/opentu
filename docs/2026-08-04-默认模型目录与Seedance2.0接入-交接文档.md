# 默认模型目录与 Seedance 2.0 接入交接文档

更新日期：2026-08-04

## 背景

默认模型入口需要与模型广场当前已开放的模型保持一致：补充 GPT-5.6、DeepSeek V4 与 Seedance 2.0 系列，同时将 GPT-5.4 及以下旧 GPT 入口从默认列表隐藏。旧模型只隐藏，不删除配置、价格、渠道或历史引用。

Seedance 2.0 使用统一 `/v1/videos` JSON 异步任务接口，不能复用 Seedance 1.x 的表单请求和旧模型名转换，因此本次单独增加 2.0 适配器、参数控件和后端透传。

## 目标行为与当前范围

- 默认入口新增 9 个模型，按统一推荐分和版本规则优先展示，并显示 `NEW`。
- GPT-5.4 及以下 18 个旧 GPT 入口只从 `default` 展示列表隐藏。
- `gpt-5.5` 保留在默认列表。
- 隐藏模型仍可解析历史会话、已保存模型和固定选择；保留对应能力的非 `default` 分组或供应商 Profile 仍可显式调用。
- 非默认供应商 Profile 的模型目录不套用默认隐藏规则。
- 新模型立即进入 `default` 可选目录，不增加灰度或额外权限开关；实际调用仍取决于 Token 分组、可用渠道、价格配置和上游能力。
- 模型名称、内部调用 ID、价格和可用路由以模型广场数据为准。

## 新增模型清单

| 类型 | 展示名称               | 内部调用 ID                       |
| ---- | ---------------------- | --------------------------------- |
| 文本 | GPT-5.6 Sol            | `gpt-5.6-sol`                     |
| 文本 | GPT-5.6 Terra          | `gpt-5.6-terra`                   |
| 文本 | GPT-5.6 Luna           | `gpt-5.6-luna`                    |
| 文本 | DeepSeek V4 Pro        | `deepseek-v4-pro`                 |
| 文本 | DeepSeek V4 Flash      | `deepseek-v4-flash`               |
| 文本 | DeepSeek V4 Flash 0731 | `deepseek-v4-flash-0731`          |
| 视频 | Seedance 2.0           | `doubao-seedance-2-0-260128`      |
| 视频 | Seedance 2.0 Fast      | `doubao-seedance-2-0-fast-260128` |
| 视频 | Seedance 2.0 Mini      | `doubao-seedance-2-0-mini-260615` |

GPT-5.6 与 DeepSeek V4 静态目录均声明工具调用能力；实际上下文、多模态输入和供应商参数仍由模型广场 endpoint 元数据及上游接口校验。

## 默认隐藏模型清单

以下模型从 `default` 可见/可调用分组视图移除，但不删除模型元数据、底层价格配置或非 `default` 分组的渠道能力：

- `gpt-5`
- `gpt-5-all`
- `gpt-5-chat-latest`
- `gpt-5-gizmo*`
- `gpt-5-mini`
- `gpt-5-nano`
- `gpt-5-pro`
- `gpt-5-pro-all`
- `gpt-5-pro-async`
- `gpt-5-thinking-all`
- `gpt-5.1`
- `gpt-5.1-all`
- `gpt-5.1-thinking`
- `gpt-5.1-thinking-all`
- `gpt-5.2`
- `gpt-5.2-all`
- `gpt-5.4`
- `gpt-5.4-mini`

`gpt-5-gizmo*` 是当前配置中的精确模型 ID，不代表通配匹配；后续出现其它 Gizmo 变体时需要显式加入隐藏清单。

## 目录与价格数据流

1. tuzi-api 读取模型广场当前的根级 `data`、`group_info`、`vendors` 和 `group_model_pricing`。
2. 对 9 个新增模型补充 `default` 展示归属；缺少 `default` 价格时，按 `原价分组`、`官方优惠`、`Codex`、其它可用分组字典序的优先级复制对应 endpoint 价格。
3. 旧 GPT 的模型元数据、底层价格配置和非 `default` 渠道能力继续保留，只从 `default` 可见/可调用分组视图移除。
4. OpenTu 将静态展示元数据与运行时模型发现结果合并，并按模型 ID 去重、按统一排序规则展示。
5. 默认 Profile 过滤旧入口；固定模型和历史选择仍通过静态模型解析恢复。

默认组没有直接渠道时，路由按以下顺序回退：

| 模型系列     | 路由来源顺序                        |
| ------------ | ----------------------------------- |
| GPT-5.6      | `原价分组` -> `官方优惠` -> `Codex` |
| DeepSeek V4  | `原价分组`                          |
| Seedance 2.0 | `原价分组`                          |

路由回退只选择执行渠道，用户仍以 `default` 分组的权限和价格语义调用。

## Seedance 2.0 前端参数边界

| 参数     | 支持范围       | 默认值 |
| -------- | -------------- | ------ |
| 时长     | 4-15 秒，整数  | 5 秒   |
| 分辨率   | `480p`、`720p` | `720p` |
| 宽高比   | `16:9`         | `16:9` |
| 生成音频 | 开启、关闭     | 开启   |
| 水印     | 开启、关闭     | 关闭   |

以下为 OpenTu 控件和请求组装约束，tuzi-api Seedance Proxy 负责透传，最终仍由上游接口校验。

参考媒体限制：

- Seedance 2.0 标准版和 Fast 在前端最多选择 9 张参考图。
- Seedance 2.0 Mini 在前端最多选择 4 张参考图。
- 支持参考视频，通过 `content[].video_url` 和 `role: reference_video` 提交。
- 视频生成弹窗提供参考音频 URL 输入、任务参数保存和编辑恢复，并按 `content[].audio_url`、`role: reference_audio` 组装请求。
- AI 输入框选中的视频可写入 Seedance 2.0 的 `input_video`；参考音频地址输入位于独立视频生成弹窗。
- 参考音频和视频只接受 HTTP(S) 或上游 `asset://` 素材地址；浏览器本地 Data URL、Blob URL 会在请求前被拒绝。

## Seedance 2.0 接口与状态流转

提交接口：

```http
POST /v1/videos
Content-Type: application/json
```

主要请求字段：

```json
{
  "model": "doubao-seedance-2-0-260128",
  "content": [],
  "resolution": "720p",
  "ratio": "16:9",
  "duration": 5,
  "generate_audio": true,
  "watermark": false
}
```

轮询接口：

```http
GET /v1/videos/{taskId}
```

状态处理规则：

- `queued`、`processing` 等中间状态继续轮询并上报进度。
- `completed`、`succeeded` 读取内联视频 URL；没有 URL 时回退 `/videos/{taskId}/content` 下载结果。
- `failed`、`error` 立即结束并展示上游业务错误。
- 网络错误、HTTP 408、425、429 和 5xx 使用有界指数退避，最多连续 10 次错误。
- 其它确定性 4xx 立即失败，不做无意义重试。
- 正常轮询基础间隔为 5 秒，最多 1080 次，约 90 分钟；同时响应任务取消信号。

Seedance Proxy 会继续透传 `content`、`resolution`、`ratio`、`duration`、`generate_audio` 和 `watermark`，避免 `480p` 在后端丢失后回退成默认分辨率。

若上游不支持新版路径，Proxy 会在 404/405 时回退旧 `/api/v1/doubao/create` 和 `/api/v1/doubao/get_result`，对外仍保持 `/v1/videos` 接口。

## 涉及模块

OpenTu：

- `packages/drawnix/src/constants/model-config.ts`
- `packages/drawnix/src/constants/video-model-config.ts`
- `packages/drawnix/src/utils/runtime-model-discovery.ts`
- `packages/drawnix/src/utils/model-sort.ts`
- `packages/drawnix/src/services/provider-routing/binding-inference.ts`
- `packages/drawnix/src/services/model-adapters/seedance2-adapter.ts`
- `packages/drawnix/src/components/ai-input-bar/workflow-converter.ts`
- `packages/drawnix/src/components/ttd-dialog/ai-video-generation.tsx`

tuzi-api：

- `setting/default_model_catalog.go`
- `model/ability.go`
- `model/pricing.go`
- `service/default_model_catalog.go`
- `service/pricing_sync.go`
- `service/channel_select.go`
- `relay/channel/task/seedanceproxy/adaptor.go`

## 性能与安全边界

- 不新增数据库实体，不迁移或删除历史模型、任务和价格数据。
- 默认目录策略使用小型静态集合，展示过滤和路由回退不引入无界缓存。
- 异步轮询具有最大次数、连续错误上限、退避上限和取消信号，避免无限请求。
- 参考图片沿用现有 URL/Data URL 兼容路径；参考音频和视频只提交服务端可访问地址，不把本地媒体编码进 JSON 请求体。
- 文档、日志和测试数据不得包含 API Key、完整鉴权头或用户敏感信息。

## 审查修复状态与剩余验证

1. **参考音频兼容已修复**：不再读取本地文件生成 Data URL，改为输入 HTTP(S) 或 `asset://` 地址；UI 和适配器均会拒绝服务端不可达的本地 URL。
2. **跨系列模型排序已修复**：`flash`、`pro`、`preview` 统一作为名称修饰词，归一后按主版本比较；Gemini 3.1、3、2.5 和同版本 Pro 优先级用例均通过。
3. **响应式输入已验证**：桌面与 390x844 窄屏下参考音频 URL 输入、清除按钮均可见且可正常清空，窄屏控件不会超出视口。
4. **剩余验证**：人工端到端 QA 尚未执行；当前环境没有 `openspec` CLI，因此 strict validate 仍未执行。

## 回滚思路

1. OpenTu 回滚新增静态模型、默认隐藏过滤和 Seedance 2.0 适配器注册。
2. tuzi-api 回滚默认目录策略与来源分组路由回退。
3. 保留旧模型、价格、渠道和历史数据，无需数据库恢复。
4. Seedance 2.0 异常时只移除新入口；Seedance 1.x 继续沿用原有 `seedance.task` 路径。

## 自动化验证

2026-08-04 已执行结果：

- OpenTu 本功能新增/定向用例 16/16 通过。
- Seedance 2.0 适配器与模型排序聚焦测试 13/13 通过。
- OpenTu 9 个受影响测试文件聚合执行为 208/236 通过、28 个失败；6 个非解析器失败与 `develop` 基线一致，22 个解析器失败来自补齐 mock 后暴露的存量断言漂移，未发现本次新增回归。
- OpenTu TypeScript 类型检查通过，Prettier 检查通过。
- OpenTu 相关文件 ESLint 已重跑；整文件检查因现有模块边界、测试空函数和测试类型标注等规则问题未通过（91 个 error、38 个 warning），新增 Seedance 2.0 适配器本身没有 ESLint error。
- tuzi-api 4 个相关 package 的 11 个聚焦用例通过，`go test ./relay/channel/task/seedanceproxy ./service ./setting ./model` 通过。
- 两个工作树的 `git diff --check` 通过。

OpenSpec CLI 当前环境不可用，因此没有在本轮重新执行 strict validate；功能规格和任务清单保留在 `openspec/changes/update-default-model-catalog/`。

## 关联 QA

- [2026-08-04-默认模型目录与 Seedance2.0 接入-人工测试文档](../qa/2026-08-04-默认模型目录与Seedance2.0接入-人工测试文档.md)

## 一句话结论

默认模型目录、旧 GPT 隐藏策略和 Seedance 2.0 JSON 异步调用链已完成整合，参考音频兼容和跨系列排序自动化阻塞已修复；人工端到端 QA 与 OpenSpec strict validate 仍待执行。
