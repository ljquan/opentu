# AI 语义分层与局部替换最终验收

**日期**：2026-09-04

**测试对象**：当前 `develop` 工作区
**局域网地址**：[http://192.168.50.225:7200/](http://192.168.50.225:7200/)

## 自动化结果

```bash
pnpm exec vitest run --environment jsdom \
  packages/drawnix/src/services/layer-decomposition \
  packages/drawnix/src/components/layer-decomposition \
  packages/drawnix/src/components/ai-input-bar/__tests__/workflow-converter.test.ts \
  packages/drawnix/src/hooks/__tests__/useAutoInsertToCanvas.test.ts \
  --reporter=dot
```

- [x] 9 个测试文件通过。
- [x] 199 个测试通过。
- [x] `pnpm exec tsc --noEmit -p packages/drawnix/tsconfig.lib.json --pretty false` 通过。
- [x] `git diff --check` 通过。

## 已验收行为

- [x] 本地分层链路优先，Seedream 仅在失败或质量不合格时兜底。
- [x] 分层结果包含背景和透明前景，不使用整画布伪前景。
- [x] 任意可解码栅格图片可进入分层流程，不限制为两张示例图。
- [x] 语义替换只更新选中的前景图层。
- [x] 替换不会覆盖背景、其他前景或原始合成图。
- [x] 新主体会重新抠图并执行旧主体残留、alpha、质量和缓存检查。
- [x] 校验失败时保持原图层和兄弟图层不变。
- [x] 一键分层、图层面板和普通生图/编辑功能保持可用。

## 局域网运行检查

- [x] 前端监听 `0.0.0.0:7200`，局域网页面可访问。
- [x] 分层服务 `GET http://127.0.0.1:8090/healthz` 返回 `status: ok`。
- [x] 健康检查显示 pipeline：`candidate_detection -> sam2 -> matting -> depth -> inpainting -> quality`。

## 人工验收清单

- [ ] 上传包含主体、背景和毛发的图片，点击一次 AI 分层，确认生成背景层和透明主体层。
- [ ] 输入“把猫换成兔子”，确认只有猫层变化，背景和其他层像素不变。
- [ ] 对透明边缘、毛发、阴影和遮挡区域进行放大检查，确认失败时不会提交半成品。
- [ ] 取消或切换画板后确认原画布不出现迟到节点。

## 已知边界

复杂毛发、反射、半透明物体和被完全遮挡的背景无法保证任意图片 100% 像素级恢复；这类结果应使用图层面板的人工校正能力复核。
