# 模型下拉缓存与空态验证

## 合并后补充（2026-09-19）

以下原验证记录保留。合并 upstream/develop b730d011 后，相关测试已纳入 13 文件、185 项通过的最终回归，drawnix 类型和 Web/SW 构建通过。全部暂存 TS/TSX 文件的 lint 存在 3 项上游原文件可复现的模块边界错误，详见 MiniMax-H3 V2 工作流验证记录。本轮未执行真实模型获取或页面交互，未提交或推送。

## 范围与原因

- 先前将下拉框自动发现扩展到自定义供应商，但保留了“存在 discoveredModels 即跳过请求”的判断。设置页发现模型但尚未确认添加时，selectedModelIds 为空，下拉框显示 0；先前测试未覆盖此组合。
- 此提前返回在 HEAD 中已存在，并非此次新增；问题是前次修补未覆盖已有缓存与选择状态之间的差异。
- 默认分组合并仅归并无 sourceProfileId 的内置模型，没有按 Midjourney 品牌删除模型。模型解析和设置页获取实现本次未改。

## 本次行为

- 显示当前类型下已发现但未添加的模型数量，允许显式添加该类型的已发现模型，保留其他类型原有选择。
- 没有当前类型模型时说明是图片、视频、音频或文本模型不可用；搜索无结果、加载中和获取失败分别反馈。
- 下拉订阅模型发现状态，主输入栏和 Agent 媒体下拉显式传入模型类型。
- 已撤回排查中额外增加的 Tuzi 下拉备用节点回退及其测试；下拉仍仅请求供应商配置的地址，设置页原有备用节点机制保持不变。
- 未修改 Key、供应商配置、生成 API 和用户已有模型选择。添加动作只在用户点击后执行。

## 实际验证

环境：macOS，本地开发工作区，服务端口 7200。

```sh
pnpm exec vitest run packages/drawnix/src/components/ai-input-bar/ModelDropdown.test.tsx packages/drawnix/src/utils/__tests__/runtime-model-discovery.test.ts packages/drawnix/src/utils/__tests__/model-grouping.test.ts
pnpm nx run drawnix:typecheck
pnpm exec eslint packages/drawnix/src/components/ai-input-bar/ModelDropdown.tsx packages/drawnix/src/components/ai-input-bar/ModelDropdown.test.tsx
git diff --check
```

- 撤回后 Vitest：3 个文件、28 项通过，覆盖未添加缓存、类型空态、添加时保留已有选择、失败退出加载及既有模型发现和分组回归。
- drawnix 类型检查通过；上述两个目标文件 ESLint 通过；差异空白检查通过。
- 测试输出存在 Node localStorage/crypto、Browserslist 和 NPM_TOKEN 环境警告；失败用例会输出预期的错误日志，未导致测试失败。
- 未使用真实 Key 的只读 HTTP 诊断：主站 OPTIONS 返回 200 并有跨域允许头；携带故意无效诊断值的 GET 返回 401，但主站和两个 CDN 节点缺少跨域允许头。美国节点的同类响应有跨域头。此证据不能证明用户 Key 失效或有效请求同样失败。

## 验收边界

- 本次没有执行浏览器自动化、生产认证模型获取或任何付费生成请求，也未跑完整构建。
- 用户实际缓存未直接读取；“81 个已发现、0 个已选”来自用户截图，代码与回归测试确认该状态可导致观察到的空列表。
- 人工验收：刷新 7200，选择图片模式的 mj原生；若缓存仍是上述状态，应显示已发现数量和添加图片模型入口，点击后展示模型。切到视频应显示该供应商暂无可用的视频模型。若实际请求失败，按显示的错误继续排查。
- 本次为局部修补，记录 QA，不另建功能 DOC。没有提交或推送。
