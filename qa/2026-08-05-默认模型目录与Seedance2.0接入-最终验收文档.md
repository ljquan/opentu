# 默认模型目录与 Seedance 2.0 接入最终验收文档

创建日期：2026-08-05

测试范围：默认模型目录、旧模型隐藏兼容、模型广场价格与路由、Seedance 2.0 官方异步接口，以及 Seedance 1.x 回归。

关联 Docs：[2026-08-05-默认模型目录与 Seedance 2.0 接入最终交接文档](../docs/2026-08-05-默认模型目录与Seedance2.0接入-最终交接文档.md)

关联 PR：

- OpenTu：[ljquan/opentu#216](https://github.com/ljquan/opentu/pull/216)
- tuzi-api：[tuziapi/tuzi-api#106](https://github.com/tuziapi/tuzi-api/pull/106)

## 1. 验收结论

当前可以确认：目录配置、Seedance 2.0 官方参数、前后端请求组装、服务端校验、计费迁移和桌面/移动页面布局已通过聚焦验证。

当前不能确认：真实上游生成、完整轮询、MP4 下载、画布插入和发布环境效果。上述项目必须使用真实测试 Token 在后端 PR 部署后执行。

## 2. 前置条件

1. tuzi-api 已部署包含 PR #106 的提交，OpenTu 使用包含 PR #216 的版本。
2. 模型广场价格同步完成，9 个新增模型具有可用 endpoint、价格和来源渠道。
3. 准备具有 `default` 分组权限的测试用户 Token。
4. 准备 GPT-5.6、DeepSeek V4 和 Seedance 2.0 可用上游渠道。
5. 准备小尺寸参考图、公网参考视频 URL，以及公网 URL、Data URL 或素材 ID 形式的参考音频。
6. 请求记录、截图和日志必须隐藏 API Key、完整 Token 和鉴权头。

当前局域网入口：`http://192.168.50.225:4174/`。

## 3. 自动化与浏览器验证记录

| 验证项                                  | 结果     | 证据或说明                                   |
| --------------------------------------- | -------- | -------------------------------------------- |
| OpenTu 官方契约聚焦测试                 | 通过     | 95/95                                        |
| OpenTu 模型排序与运行时发现             | 通过     | 18/18                                        |
| OpenTu 最终审阅聚焦测试                 | 通过     | 19/19                                        |
| OpenTu TypeScript、Prettier、diff 检查  | 通过     | 均为退出码 0                                 |
| OpenTu 核心 ESLint                      | 通过     | 0 error，3 个既有 warning                    |
| 桌面浏览器                              | 通过     | Chromium 1440x900，无横向溢出                |
| 移动浏览器                              | 通过     | Chromium 390x844，无横向溢出                 |
| Seedance 2.0 参数控件                   | 通过     | 9 个时长、3 个分辨率、7 个比例及全部可选参数 |
| 浏览器 `POST /v1/videos` JSON 拦截      | 通过     | 字段和值类型符合官方契约，未发送真实上游     |
| tuzi-api 模型、目录和 Seedance 聚焦测试 | 通过     | 普通测试与两个适配器 race 测试通过           |
| tuzi-api 计费迁移稳定性                 | 通过     | 连续 10 次通过                               |
| tuzi-api 默认模型列表控制器回归         | 通过     | 本地通过，远端 CI 关联失败已消失             |
| 真实 Seedance 创建、轮询、下载          | 未执行   | 需要真实测试 Token 和已部署后端              |
| OpenSpec strict validate                | 未执行   | 当前环境没有 OpenSpec CLI                    |
| OpenTu 全量 CI                          | 未通过   | 存量聚合测试、size-limit 和 Vercel 授权失败  |
| tuzi-api 全量 Model Call CI             | 部分通过 | 4/6，通过项与剩余未触及失败见本文第 10 节    |

## 4. 默认模型目录验收

### 4.1 新增模型

在默认 Profile 中依次打开文本和视频模型选择器。

- [ ] `gpt-5.6-sol` 展示为 GPT-5.6 Sol，并显示 `NEW`。
- [ ] `gpt-5.6-terra` 展示为 GPT-5.6 Terra，并显示 `NEW`。
- [ ] `gpt-5.6-luna` 展示为 GPT-5.6 Luna，并显示 `NEW`。
- [ ] `deepseek-v4-pro` 展示为 DeepSeek V4 Pro，并显示 `NEW`。
- [ ] `deepseek-v4-flash` 展示为 DeepSeek V4 Flash，并显示 `NEW`。
- [ ] `deepseek-v4-flash-0731` 展示为 DeepSeek V4 Flash 0731，并显示 `NEW`。
- [ ] `doubao-seedance-2-0-260128` 展示为 Seedance 2.0，并显示 `NEW`。
- [ ] `doubao-seedance-2-0-fast-260128` 展示为 Seedance 2.0 Fast，并显示 `NEW`。
- [ ] `doubao-seedance-2-0-mini-260615` 展示为 Seedance 2.0 Mini，并显示 `NEW`。
- [ ] GPT-5.6、DeepSeek V4、Seedance 2.0 分别排在各自旧系列之前。

### 4.2 旧模型隐藏与兼容

在默认选择器中搜索 Docs 第 3 节列出的 18 个旧 ID，并打开包含旧模型的历史会话和非默认 Profile。

- [ ] 18 个旧模型不出现在 `default` 新建选择器中。
- [ ] `gpt-5.5` 仍在 `default`。
- [ ] 历史会话和固定模型仍能解析旧 ID，不删除记录。
- [ ] 非 `default` Profile 不被默认隐藏规则误过滤。
- [ ] 保留对应渠道时，非 `default` 分组仍可显式调用旧 ID。

## 5. 模型广场、价格与路由验收

完成一次模型广场同步后，查看 9 个模型的 endpoint、价格和调用分组。

- [ ] 9 个模型均有 `default` 展示归属和价格。
- [ ] 展示名称和内部 ID 与 Docs 清单一致。
- [ ] Seedance 三个型号只展示 480p、720p、1080p 计费，不展示 4K。
- [ ] 旧内置计费规则升级后补齐 Fast/Mini 1080p。
- [ ] 当前规则、局部修改和自定义 `rule_version` 不被覆盖。
- [ ] 旧隐藏模型的全局价格和非 `default` 分组价格仍保留。
- [ ] `default` 没有直接渠道时，GPT-5.6 按 `原价分组`、`官方优惠`、`Codex` 回退。
- [ ] DeepSeek V4 和 Seedance 2.0 只回退到已配置的 `原价分组`。
- [ ] 路由回退不改变用户的 `default` 权限和计费语义。

## 6. GPT-5.6 与 DeepSeek V4 调用验收

分别使用六个文本模型提交普通对话和一次工具调用。

- [ ] 请求使用清单中的真实内部 ID，不改写为旧模型。
- [ ] 普通文本响应成功，使用日志记录真实模型 ID。
- [ ] 工具调用不会被前端静态能力错误拦截。
- [ ] 上下文、多模态和供应商专属参数由 endpoint 元数据及上游校验，不因静态描述被错误放宽。

## 7. Seedance 2.0 参数验收

依次选择 Seedance 2.0、Fast 和 Mini，展开参数面板。

- [x] 时长显示 4、5、6、7、8、9、10、11、12 秒，默认 5 秒。
- [x] 分辨率独立显示 1080p、720p、480p，默认 720p。
- [x] 比例独立显示 16:9、4:3、1:1、3:4、9:16、21:9、adaptive，默认 16:9。
- [x] 生成音频默认开启，水印默认关闭。
- [x] 随机种子支持整数输入，固定镜头支持开启和关闭。
- [x] 桌面和移动端控件可访问，无横向溢出或遮挡。

## 8. Seedance 官方请求验收

### 8.1 最小请求

提交文本、5 秒、720p、16:9 的任务。

- [ ] 请求方法为 `POST /v1/videos`。
- [ ] `Content-Type` 为 `application/json`。
- [ ] `model` 使用选中的 `doubao-seedance-2-0-*` ID。
- [ ] `duration` 为数字，`generate_audio` 和 `watermark` 为布尔值。
- [ ] 返回 2xx 和可轮询的任务 ID。

### 8.2 参数边界

分别提交以下组合：

1. 4 秒、480p、16:9、`seed: 0`、`camera_fixed: false`。
2. 12 秒、1080p、adaptive、生成音频关闭、水印开启。

- [ ] OpenTu JSON 保留所选值和正确类型。
- [ ] tuzi-api 映射后校验不改写合法值。
- [ ] `seed: 0` 和 `camera_fixed: false` 不丢失。
- [ ] 两条后端 Seedance 路由行为一致。

### 8.3 非法参数

尝试 3 秒、13 秒、小数时长、4K、832 宽度、未知比例和字符串布尔值。

- [ ] OpenTu 可识别的非法值在提交前被拦截。
- [ ] 绕过前端直调时，tuzi-api 在映射后、计价前返回稳定 4xx。
- [ ] 非法请求不进入上游、不预扣费用、不创建任务。

## 9. 参考媒体与异步状态验收

### 9.1 参考图片、视频和音频

- [ ] 参考图片使用 `image_url` 与 `role: reference_image`。
- [ ] 公网参考视频使用 `video_url` 与 `role: reference_video`。
- [ ] 私网、本机、Blob、Data URL 参考视频被拒绝。
- [ ] 公网音频 URL、合法音频 Data URL 和素材 ID 使用 `audio_url` 与 `role: reference_audio`。
- [ ] 音频 Data URL 非法 MIME、非法 Base64 或超过 16 MiB 时被拒绝。
- [ ] 移除参考音频后，请求不再包含 `audio_url`。
- [ ] 编辑或重试任务时，参考媒体和参数不会丢失。

### 9.2 轮询、完成与失败

- [ ] 使用 `GET /v1/videos/{taskId}` 轮询同一个任务。
- [ ] `queued`、`processing` 会更新任务队列进度。
- [ ] `completed/succeeded` 优先读取内联 URL，无 URL 时读取内容接口。
- [ ] 最终 MP4 可下载并插入画布。
- [ ] `failed/error` 立即结束并展示脱敏后的上游错误。
- [ ] 网络错误、408、425、429 和 5xx 使用有界退避。
- [ ] 普通 400、401、403、404 不重试、不重复创建任务。
- [ ] 取消任务后停止轮询。

## 10. Seedance 1.x 回归

分别选择 Seedance 1.5 Pro、1.0 Pro、1.0 Fast 和 1.0 Lite，提交原有首帧、尾帧或参考图任务。

- [ ] 1.x 继续使用原请求格式和模型名转换。
- [ ] Seedance 2.0 JSON 适配器不匹配 1.x。
- [ ] 原有参数、轮询、结果下载和画布插入不变。

## 11. 当前 CI 风险

OpenTu：

- 主 CI 因存量聚合测试和 AI Chat size-limit 失败。
- Vercel 因项目授权失败；Netlify Deploy Preview 成功。
- 本功能聚焦测试、类型检查、格式检查和浏览器验证通过。

tuzi-api：

- 通过：关键模型调用 race、并发计费、Redis 限流、离线负载。
- 未通过：TaskCenter 路由测试仍期望旧 `AdminRoute`；SecurityHeaders 未取得预期响应头；3 个 deployment-readiness 环境用例失败；图片物化测试无法创建 `/www` 缓存目录。
- 本次新增 default 模型导致的控制器测试预期问题已经修正，最新远端日志中该失败已消失。

不能将上述全量 CI 记录写成全部通过，也不应在本任务中顺带修改未触及模块。

## 12. 人工测试记录

| 场景                             | 当前状态 | 测试人 | 证据                       |
| -------------------------------- | -------- | ------ | -------------------------- |
| 9 个新增模型名称、ID、NEW 与排序 | 待联调   |        |                            |
| 18 个旧模型隐藏与历史兼容        | 待联调   |        |                            |
| 模型广场价格和来源分组路由       | 待联调   |        |                            |
| GPT-5.6 与 DeepSeek V4 实际调用  | 待联调   |        |                            |
| Seedance 2.0 桌面参数控件        | 通过     | AI     | Chromium 1440x900          |
| Seedance 2.0 移动参数控件        | 通过     | AI     | Chromium 390x844           |
| Seedance 2.0 JSON 请求组装       | 通过     | AI     | 浏览器拦截，未请求真实上游 |
| 参考媒体真实上游提交             | 待联调   |        |                            |
| 异步创建、轮询、下载和画布插入   | 待联调   |        |                            |
| Seedance 1.x 实际回归            | 待联调   |        |                            |

失败记录模板：

```text
测试场景：
环境 / 版本：
账号分组 / 供应商 Profile：
展示模型名 / 内部模型 ID：
时长 / 分辨率 / 比例：
参考图片 / 视频 / 音频：
请求 ID / 任务 ID：
实际请求字段（脱敏）：
轮询状态与 HTTP 状态码：
预期结果：
实际结果：
错误日志（隐藏密钥和鉴权头）：
截图或录屏：
```

## 13. 发布门槛

- [ ] tuzi-api PR #106 已合并并部署到测试环境。
- [ ] 9 个新增模型的模型广场价格与来源渠道可用。
- [ ] 六个文本模型至少完成一次普通对话，工具模型至少完成一次工具调用。
- [ ] Seedance 三个型号至少各完成一次真实创建、轮询和下载。
- [ ] 4 秒/480p 与 12 秒/1080p 两组边界请求通过。
- [ ] `seed: 0`、`camera_fixed: false`、音频关闭和水印开启透传正确。
- [ ] 参考视频、音频 URL、音频 Data URL 和素材 ID 完成正反例验证。
- [ ] Seedance 1.x 回归通过。
- [ ] 已知 CI 红灯经维护者确认不阻断本次合并，或已由独立变更修复。
- [ ] OpenTu PR #216 完成人工验收后合并。
