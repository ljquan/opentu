# Tasks: 任务灵宠提醒

## 1. Preference And Event Core

- [x] 1.1 在现有 `AppSettings` 中新增版本化灵宠偏好类型、默认值、规范化和监听访问器
- [x] 1.2 验证灵宠偏好随现有环境备份导出、恢复，且不进入敏感配置
- [x] 1.3 新增 IMAGE/VIDEO/CHAT 类型映射、状态迁移、进度去重和并发终态聚合
- [x] 1.4 按 `(task.id, task.startedAt)` 区分重试运行，并从 `event.task.status` 识别终态
- [x] 1.5 新增复用现有 TTS 设置的短消息播报器，并处理不支持、播放失败与共享语音冲突降级

## 2. User Interface

- [x] 2.1 在左侧工具栏垃圾桶按钮正上方新增稳定尺寸的灵宠入口、状态气泡和无障碍状态文本
- [x] 2.2 为 idle、wave、running、waiting、review、jumping、failed 增加动作样式
- [x] 2.3 支持减少动态效果、窄视口气泡约束和相邻工具按钮避让
- [x] 2.4 在设置对话框新增“任务灵宠”页，提供显示、动作、语音和任务类型开关
- [x] 2.5 按现有工具栏拆分方式加载灵宠逻辑，避免把任务队列重型面板嵌入按钮

## 3. Verification

- [x] 3.1 单测覆盖默认值、非法配置、旧配置兼容、存储异常和变更通知
- [x] 3.2 单测覆盖 CHAT/IMAGE/VIDEO 过滤、状态映射和进度区间去重
- [x] 3.3 单测覆盖并发完成/失败聚合、同运行重复终态、重试运行和清理释放
- [x] 3.4 组件测试覆盖空任务、初始活动任务、进行中、完成、失败、语音不可用/占用和设置切换
- [x] 3.5 聚焦类型检查与现有任务队列、TTS、环境备份回归验证
- [x] 3.6 桌面与移动视口手动验证布局、动画、减少动态效果和设置持久化

## 4. Movable Pet Revision

- [x] 4.1 将工具栏兔子本体替换为灵宠设置入口，并直达设置对话框的任务灵宠页
- [x] 4.2 将灵宠本体挂载为独立画布浮层，复用品牌兔子并提升裁切、尺寸与气泡视觉
- [x] 4.3 实现指针拖动、左右移动反馈、视口边界夹取和松手后位置持久化
- [x] 4.4 将灵宠设置升级为 v2，兼容迁移 v1 并校验归一化位置
- [x] 4.5 补充设置入口、拖动、响应式位置与设置迁移测试
- [x] 4.6 重新执行桌面、移动视觉验收与必要回归验证
