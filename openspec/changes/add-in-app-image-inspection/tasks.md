## 1. 巡检核心

- [x] 1.1 新增纯函数矩阵生成器，遍历各分组并仅覆盖指定的 GPT Image 2 与 Gemini 模型白名单。
- [x] 1.2 新增尺寸解析与校验模块，支持精确尺寸、比例、1K/2K/4K/HD 档位及 URL 计算公式。
- [x] 1.3 将巡检运行迁移到 Tuzi 服务端持久后台任务，支持创建、恢复、停止和单用例异常隔离。
- [x] 1.4 服务端分页保存巡检会话与结果元数据，不保存图片二进制。
- [x] 1.5 服务端每天 07:00 按中国法定工作日幂等调度，页面关闭时继续运行。

## 2. OpenTu 内部界面

- [x] 2.1 注册独立“生图巡检报表”内部工具并接入现有工具窗口系统。
- [x] 2.2 展示运行范围、用例数、进度、通过/失败统计和停止控制。
- [x] 2.3 展示实际图片、URL、任务 ID、模型/分组、请求尺寸、实际尺寸、耗时、状态和公式。
- [x] 2.4 支持导出 XLSX、JSON；报表界面不展示 dry-run 计划列表。
- [x] 2.5 在 OpenTu 左侧工具栏提供独立巡检图标，点击后直接最大化报表并自动运行一次；运行中重复点击只打开当前报表。
- [x] 2.6 删除页面内 07:00 定时器，改为服务端自动调度；前端断网指数退避并在恢复网络/可见时立即同步。
- [x] 2.7 报表改为服务端运行和用例分页，重复点击只打开并复用当前运行。

## 3. 验证

- [x] 3.1 单元测试覆盖矩阵生成、GPT 精确尺寸、Gemini 档位和 4K 降档失败。
- [x] 3.2 验证报表从当前 OpenTu 左侧工具栏打开，不创建独立浏览器或标签页；真实任务入栏待单用例联调继续验证。
- [ ] 3.3 验证至少一个 GPT Image 2 和一个 Gemini 动态模型返回真实图片 URL 并写入报表。
- [ ] 3.4 验证普通模型评测和普通生图流程无回归。
- [x] 3.5 停用旧外部定时任务并更新使用说明。
- [x] 3.6 将指定模型白名单收敛为唯一数据源，在报表中明示范围，并逐模型校验用例数与分辨率档位。
- [x] 3.7 运行前同步所有可发现分组，分组发现失败时不带缺口启动；严格匹配模型 ID，并校验实际图片、任务元数据与 URL 尺寸冲突。
- [x] 3.8 加固长时间运行：服务端重启恢复、单用例异常隔离、前端轮询退避、刷新恢复与报表错误边界已完成。
- [ ] 3.9 验证关闭/刷新 OpenTu、短时断网及服务端重启不会丢失或中断运行。
- [x] 3.10 发布前加固：按 Token/用户权限过滤分组和模型，自动调度仅使用显式 Token ID，并自动清理终态运行残留的活动幂等键。
- [x] 3.11 加固永久等待与停止响应：单用例默认 120 分钟超时，重启恢复不重置计时；超时不重复提交真实任务，用户停止后当前轮询用例及时结束。
- [x] 3.12 加固前端半开连接与卸载清理：普通请求 45 秒截止、导出 5 分钟截止，关闭报表时取消启动/停止/导出和轮询请求，避免轮询永久停摆或卸载后泄漏。
- [x] 3.13 限制图片预览内存：同一时间仅挂载一张实际图片，切换或收起预览后立即卸载，避免多张 2K/4K 图片同时解码导致页面假死。
- [x] 3.14 加固多 Worker 与崩溃恢复：内部提交使用稳定请求 ID 对账，慢提交期间持续续租；运行汇总串行锁定且每分钟对账，避免重复生图、终态回退或残留活动运行。
- [x] 3.15 加固长期后台恢复：提交中关机和 panic 归还租约，任务对账错误不重复提交；已有稳定请求可跨多次重启恢复，运行汇总分页覆盖全部活动运行。
- [x] 3.16 修正尺寸兜底：GPT 精确矩阵不叠加通用长边阈值；自然尺寸读取失败时用 URL 编码尺寸识别明确降档，但仍保留待复核警告语义。
- [x] 3.17 同步 default 分组 Image 2 特殊规则：仅按最大公约数精确约分比例判定，其他分组与 Gemini 规则不变。
