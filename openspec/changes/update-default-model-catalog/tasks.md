## 1. OpenSpec

- [x] 1.1 核对现有运行时模型发现、统一排序和协议路由规格
- [x] 1.2 定义默认入口隐藏但保留历史解析的行为

## 2. 默认模型目录

- [x] 2.1 增加 GPT-5.6、DeepSeek V4 与 Seedance 2.0 系列静态模型元数据
- [x] 2.2 隐藏 GPT-5.4 及以下旧默认入口，并保留历史解析
- [x] 2.3 验证新增模型按统一规则优先展示

## 3. Seedance 2.0

- [x] 3.1 增加标准版、Fast、Mini 的时长、独立分辨率/比例与输入控件配置
- [x] 3.2 让 2.0 系列走 `/v1/videos` 异步调用且不进入 1.x 模型名转换
- [x] 3.3 验证提交、轮询、结果读取和错误退避行为
- [x] 3.4 参考音频接受官方 URL、有效且有界的音频 Data URL 与素材 ID，参考视频只接受公网 URL
- [x] 3.5 透传官方 `seed`、`camera_fixed` 并保留旧组合尺寸任务兼容
- [x] 3.6 服务端严格校验 4-12 秒、3 种分辨率与 7 种比例，拒绝枚举外值

## 4. Verification

- [x] 4.1 运行模型目录、排序和运行时发现聚焦测试
- [x] 4.2 运行 Seedance 与共享视频 API 聚焦测试
- [x] 4.3 运行 TypeScript 类型检查或等价聚焦检查（已执行并通过）
- [ ] 4.4 严格校验 OpenSpec 变更（当前环境没有 `openspec` CLI）
- [x] 4.5 桌面与移动端检查独立分辨率/比例控件，并拦截验证实际请求体
