## 1. Request ID 提交

- [x] 1.1 所有图片入口传递稳定任务 Request ID
- [x] 1.2 正式 POST 前事务持久化提交 ID、提交标记和调用路由
- [x] 1.3 分离可信节点与 Request-ID-CORS 兼容节点
- [x] 1.4 所有正式请求严格保留用户配置的 Base URL
- [x] 1.5 配置节点不兼容 Request ID 时省略请求头且只提交一次
- [x] 1.6 GET 和第三方目标不携带任务 Request ID

## 2. 刷新恢复

- [x] 2.1 仅识别具有完整新提交元数据的处理中图片任务
- [x] 2.2 页面恢复事务切换为 `PROCESSING + POLLING`
- [x] 2.3 实现只读结果查询、有界并发、退避、超时和资源清理
- [x] 2.4 保留同 profile/model 的失效 binding 安全重解析
- [x] 2.5 成功结果复用缓存、任务完成和卡片渲染流程
- [x] 2.6 Request ID 条件事务保证首个终态生效
- [x] 2.7 取消、删除、重试与旧结果保持幂等
- [x] 2.8 启动扫描自动唤醒延迟运行时并使用有限重试
- [x] 2.9 正常 15 分钟到期明确失败，不提供延长补偿
- [x] 2.10 设置初始化 pending/reject 保留原截止并写入明确恢复错误
- [x] 2.11 同 ID 覆盖、取消与重试使用执行实例令牌阻止旧回调和旧清理
- [x] 2.12 运行期恢复视频自动接管、失效轮询停止请求且路由缺失明确失败

## 3. 范围清理与验证

- [x] 3.1 删除健康请求实时并行轮询
- [x] 3.2 同步图片 fetch 或成功响应体网络中断切换为只读恢复且不重复 POST，其他图片协议不误入同步恢复
- [x] 3.3 删除旧任务猜测迁移与 24 小时补偿
- [x] 3.4 删除同源代理与自动节点改写
- [x] 3.5 精简定向测试、Docs、QA 与 OpenSpec
- [x] 3.6 运行定向测试、类型检查、OpenSpec strict 和差异检查
- [x] 3.7 禁止 GET、HEAD、POST 和恢复查询跨节点 fallback
- [x] 3.8 等待设置解密并按实际 prepared 请求和 binding 判断恢复资格
- [x] 3.9 将恢复查询、结果缓存和终态写回纳入同一有界并发与取消链路
- [x] 3.10 收口响应体硬超时、大 JSON 解析与网络错误分类
- [x] 3.11 校验恢复响应 Request ID、启动失败和持久化竞态终态
- [x] 3.12 删除不可达状态与测试噪声并补充真实入口回归测试
- [x] 3.13 限制 Custom HTTP multipart 图片来源、凭据、重定向、MIME、数量、字节与取消链路
- [x] 3.14 覆盖设置初始化、abort-ignoring CHAT/音频、fallback 文本/视频与 replacement 竞态回归
