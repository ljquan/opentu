# Tasks: 图片任务按 Request ID 自动恢复轮询

## 1. 查询与调度

- [x] 1.1 实现可信 Tuzi 图片结果查询与响应解析，使用当前持久化的 `submissionRequestId`，旧任务回退到 `task.id`
- [x] 1.2 复用任务调用路由解析当前认证信息，并确保 GET 不携带 `X-Request-Id`
- [x] 1.3 实现四个 Request-ID 节点的只读查询容错
- [x] 1.4 实现有界并发、FIFO 等待队列、抖动/退避和完整资源清理
- [x] 1.5 正式 POST 保持用户原配置节点，网络错误或 `404` 不跨节点重复提交

## 2. 任务生命周期接入

- [x] 2.1 任务队列与独立媒体生成入口都在事务提交后再发送正式 POST；正式 POST 已尝试后的模糊错误进入 `PROCESSING + POLLING`，提交前错误直接失败
- [x] 2.2 页面初始化时使用持久化提交 Request ID 恢复处理中任务，以及有效期内缺少新字段的旧版 `PROCESSING`/`INTERRUPTED`/`INTERRUPTED_DURING_SUBMISSION` 任务
- [x] 2.3 成功结果复用现有缓存、任务完成、批量预览和画布插入流程
- [x] 2.4 上游失败、认证失败和恢复超时写回明确错误
- [x] 2.5 取消、删除、重试与迟到响应保持幂等；重试保持任务 ID、生成新 Request ID 并停止旧轮询
- [x] 2.6 完成、失败与异步 `remoteId` 使用同一 IndexedDB 事务校验当前 Request ID，旧尝试不得覆盖新重试或既有终态

## 3. 验证

- [x] 3.1 覆盖网络中断、页面刷新后最终成功，以及旧版 `PROCESSING`/`INTERRUPTED` 任务迁移测试
- [x] 3.2 覆盖上游失败、401/403、原始 404/5xx 节点切换和超时测试
- [x] 3.3 覆盖 20 个以上批量恢复任务全部排队且不丢失
- [x] 3.4 覆盖取消、删除、重试、原请求与轮询竞态
- [x] 3.5 覆盖缓存失败仍以远程 URL 完成，且不重复下载
- [x] 3.6 覆盖非 Tuzi/绝对外部地址不触发查询或凭据泄漏
- [x] 3.7 运行定向测试、类型检查与 `git diff --check`
- [x] 3.8 覆盖首次 Request ID 等于任务 ID、每次重试生成不同 ID、旧轮询/旧执行不能覆盖新重试
- [x] 3.9 覆盖参考图预处理等提交前错误不进入恢复，且提交标记在正式 POST 前完成持久化
- [x] 3.10 覆盖独立媒体生成入口恢复、IndexedDB 事务提交边界与旧 Request ID 条件写回
- [x] 3.11 覆盖公网、第三方域名和局域网入口保持原提交 URL，且网络错误、`404` 不跨节点重提

## 4. 交付

- [x] 4.1 更新 OpenSpec 任务勾选状态
- [x] 4.2 提交中文 Git Log 并推送当前 PR 分支
- [x] 4.3 在本地、局域网和公网来源验证恢复行为
